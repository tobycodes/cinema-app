import React, { FC } from 'react';
import './index.scss';

import Content from '../content';

const Main: FC = () => {
  return (
    <div className="main">
      <Content />
    </div>
  );
};

export default Main;
